# add-airline-domain-and-agent

Foundational change: airline domain (in-memory mutable DB from tau2 db.json), LangGraph agent with full policy.md system prompt, 14 langchain @tool wrappers over the store, Chainlit chat UI for interactive testing, and Langfuse tracing. No simulator or evaluator yet — those land in a follow-up change.
